# RMSteg / Lite-RMSteg 项目依赖
# 安装方式：pip install -r requirements.txt

# 核心深度学习框架
# 注意：PyTorch 需根据 CUDA 版本单独安装，详见下方说明
# torch>=1.13.0
# torchvision>=0.14.0
# torchaudio>=0.13.0

# 图像处理
Pillow>=9.0.0
opencv-python>=4.6.0

# 数据处理与科学计算
numpy>=1.21.0
scipy>=1.7.0

# 配置文件处理
PyYAML>=6.0
easydict>=1.10

# 张量操作
einops>=0.6.0

# 二维码生成与解码
qrcode[pil]>=7.3.1
pyzbar>=0.1.9

# 感知质量评估
lpips>=0.1.4

# 训练监控与可视化
tqdm>=4.64.0
tensorboard>=2.10.0
matplotlib>=3.5.0

# ============================================================
# PyTorch 安装说明（根据 CUDA 版本选择）
# ============================================================
# 
# 方式1: CPU 版本（仅用于测试，不推荐训练）
# pip install torch torchvision torchaudio
#
# 方式2: CUDA 11.8（推荐，本项目测试环境）
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 方式3: CUDA 12.1
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# 方式4: 其他 CUDA 版本
# 访问 https://pytorch.org/get-started/locally/ 选择对应版本
#
# ============================================================
# 快速安装（推荐步骤）
# ============================================================
#
# 1. 创建虚拟环境
#    conda create -n steg_project python=3.9
#    conda activate steg_project
#
# 2. 安装 PyTorch (CUDA 11.8 示例)
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 3. 安装其他依赖
#    pip install -r requirements.txt
#
# 4. (可选) 使用清华镜像加速
#    pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# ============================================================
